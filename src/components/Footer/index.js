import {useRef} from "react";

const index = () => {
    return (
        <div className={"w-full flex justify-between items-center"}>
            Footer
        </div>
    );
};

export default index;